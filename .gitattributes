# 协议、状态和审计账本统一 LF，避免不同 Git/Windows 配置改变逐行证据的字节边界。
* text=auto
.gitattributes text eol=lf
.gitignore text eol=lf
*.mjs  text eol=lf
*.js   text eol=lf
*.json text eol=lf
*.jsonl text eol=lf
*.md   text eol=lf
*.log  text eol=lf
*.sh   text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf

# 无扩展名，会落进 `* text=auto` 被按平台改行尾——那会让锚定的哈希在别人 clone 后对不上。
LICENSE text eol=lf

# OpenTimestamps 证明是二进制。此前它们没被改写，靠的是 git 的二进制启发式恰好识别出 NUL 字节——
# 也就是靠运气不是靠守卫（论文案例 14 里 audit.log 留在版本控制外，也是同一种运气）。
# 一个不含 NUL 的 .ots 会被当文本改写，证明当场作废，且没有任何东西会报错。所以显式钉死。
*.ots  binary

# 文学语料是「逐字节即证据」的东西：仓库里到处在记它们的 sha256（学历 facts、
# fetch-corpus.mjs 清单、CORPUS-SOURCES.md）。而 `* text=auto` 会在 Windows 上把 LF 换成 CRLF，
# 于是别人 clone 下来一算指纹全对不上，**且没有任何东西会报错**——跟上面 .ots 那条是同一个病：
# 此前没出事只是因为 git 的启发式恰好没动它，那是运气不是守卫。
demo/*/corpus/** binary
demo/hongloumeng-c/handoff/blind/corpus/** binary

# Windows 原生命令脚本保留 CRLF；压缩包、Office、CAD 与图片按二进制处理。
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.docx binary
*.xlsx binary
*.pptx binary
*.dwg  binary
*.dxf  binary
