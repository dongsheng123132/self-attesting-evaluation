# 公开边界（publication policy v0.1 · 2026-08-10）

> 这份文件存在的原因：2026-08-10 首次公开时，「什么能出去」全靠临时判断，
> 仓库里没有任何一份文件写下这条线。论文案例 15 说的就是这个——
> **一条规则有机制、有主体，却没有边界，于是「它适不适用于这里」这个问题从来没人问过。**
> 所以边界写在这里，且由 `governance/publish.mjs` 的闸门执行，二者由判据 A8.1 对齐。

## 一句话

**协议和判据全公开，实验证据全公开，隐藏判据集与客户数据永不公开，商业结论先内后外。**
理由不是保守或开放的偏好，是三类资产的价值来源不一样。

---

## 一、永远公开（价值来自被采纳）

| 资产 | 为什么必须公开 |
|---|---|
| 七个部件的协议与实现（本象/本境/影核/北桥/南桥/学堂/治理） | 协议的价值等于采纳率。闭源协议是矛盾修辞——没人能采纳一个看不见的标准 |
| 285 条判据与全部验证器 | 「可执行而非建议」这句话，不给人跑就是空话 |
| RFC-0004 … RFC-0009 | 同上 |
| 论文与证据锚点（`governance/anchors/`） | 论文的全部价值在于可核 |
| 本境备份 `demo/.benjing-backups/`（客户项目除外） | 论文案例 8–13 的唯一凭证。不公开则这些案例退回自证 |
| 只追加账本（`audit.log` / `observations.jsonl` / `crosscheck.jsonl`） | 跨模型复算靠它们按行切片 |

**推论**：不要因为「代码不好看」或「历史里有错」而不公开。撤回记录本身是资产——
论文里三次公开撤回自己结论，那是买不到的可信度。

---

## 二、永不公开（泄露即失效或即侵权）

| 资产 | 为什么 | 机制 |
|---|---|---|
| **隐藏判据集 H**（`demo/*/hidden/`） | 基准的命根子。判据一旦可见，被测者就能对着它写，基准当场作废且**无法通过再出一版修复**（模型会记住） | `.gitignore` + `publish.mjs` BLOCKER `hidden-judgeset` + 只提交 `SEAL.json` 哈希 |
| **客户真实工作区**（`demo/book-project/`、`private/`） | 是别人的东西，不是我们的 | `.gitignore` + BLOCKER `client-workspace` / `private-dir` |
| **会话记录与导出**（`.claude/trace.jsonl`、`ChatGPT-*.md`） | 私人工作内容。基线提交曾带进来一份 84K 的导出并在每个提交里躺了 21 轮（案例 17） | `.gitignore` + BLOCKER `session-transcript` |

**这三条不接受「先推上去再删」**。公开仓库上没有删除这回事，只有「你以为删了」。
所以闸门扫的是**全部可达 git 对象**，不是工作树。

上表的机器可读形式。判据 A8.1 拿这一行与 `governance/publish.mjs` 的 `BLOCKERS` 双向比对
——**契约不解析散文**：第一版判据靠正则从上面的表格里抠 id，抠出 3 个漏了 1 个，
于是「双向校验」有一边只查了四分之三，而它看起来是全的。

```
never-public-blockers: session-transcript, client-workspace, private-dir, hidden-judgeset
```

---

## 三、先内后外（时机问题，不是保密问题）

| 资产 | 处理 |
|---|---|
| **证明轨的因果结论**（`demo/holmes-untold`：持久状态能否替代模型规模） | 预注册文书、判据规格、封条**现在就公开**；结论等实验跑完再公开，**跑之前不许放风** |
| **中文文学判分器**（红楼梦判词兑现度、格律可判性） | 规格与判分器公开（不给人跑就没人信）；具体判据集分 V/H，H 走第二条 |
| **组装机三条继承主张**（`ACCEPTANCE.md`） | 已可公开，但值得先写成论文再发，而不是以验收报告的形态散着 |

---

## 四、商业价值在哪（写下来，免得把力气花错地方）

不在代码。代码全公开，反而是护城河的一部分——**别人抄走 7000 行也复现不了那 285 条判据背后的十九次失败**。

真正的商业资产是三样：

1. **证明轨的结论**。如果「持久状态可以替代模型规模」被严格证出来，那是定价权：
   它直接对应「小模型 + 好 harness」相对「大模型」的成本结构，也就是 uking 的整个商业逻辑。
   **注意：这个结论现在还不存在。** 预注册的四个生效条件只满足 1 个，一次生成都还没跑，也不许跑。
   把没跑出来的结论拿去讲，是把最值钱的东西换成最便宜的东西。
2. **隐藏判据集 + 判分器**。可机器判分的中文长文本生成基准是稀缺物。判分器公开建立信任，
   H 保密维持基准效力——这是评测服务的标准结构，两边缺一不可。
3. **uking 产品本身**（闭源，不在本仓库）。

---

## 五、这份文件怎么被执行

写下来的政策如果没有机制，就是案例 14（政策与它的违反在同一个提交里）。所以：

- 第二节每一条都对应 `governance/publish.mjs` 里一条 BLOCKER
- 判据 **A8.1** 断言二者一一对应：本文件列出的「永不公开」类别，必须在闸门里都有实现；
  闸门里有而本文件没写的，同样报红——**一个没写进政策的拦截规则，和一个没实现的政策条款，一样危险**
- 发布只走 `node governance/publish.mjs --push`，不走 `git push`

## 六、这份文件的局限

- 第三节「先内后外」没有机制，只有纪律。目前靠人记得，这是已知缺口。
- 第一节「永远公开」也没有机制：没有任何东西会在我们**漏发**某样该公开的东西时报警。
  漏发比误发安静得多，按论文 class E，它大概率被低估。
