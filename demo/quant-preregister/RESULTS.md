# 结果 — 回测挖掘对照

**预注册在本文件存在之前已进比特币日历**：锚点 `63d46b16fc99c421`，commit `a5e60eb`。

> ✅ **外部时间锚已确认**：该锚点已进比特币区块，块内时间 **2026-08-10T16:20:54Z**
> （`node governance/anchor.mjs upgrade` 复核）。**这个时间不是我们写的**——本仓库的 git
> 没有 remote，commit date 可以任意伪造，而区块头不能。任何陌生人都可以独立核验：
> 这份预注册的指纹在那一刻之前就已存在，而下面的结果是之后才产生的。
判定规则由 [`prereg.json`](prereg.json) 给出、由跑手自动裁决，本文件只照抄，不加叙事。

> **四条判据两条成立、两条不成立。** 不成立的那两条按预注册原样报告，没有改阈值。

---

## 1. 数字

20 个种子 · 36 组参数网格 · 仪器失效 0 · 零方差 0 · 丢弃种子 0

| 臂 | 在哪选参 | 汇报的年化夏普 | 标准差 |
|---|---|---|---|
| **F 自由挖掘** | 全样本 | **0.303** | 0.321 |
| **P 预注册** | 仅样本内 → 报样本外 | **−0.192** | 0.566 |
| **N 地板** | 不看数据 → 报样本外 | **−0.049** | 0.522 |
| *（对照）P 的样本内成绩* | — | *0.476* | *0.424* |

- 挖掘溢价 `F − P` = **0.494855**
- 纯噪声下 36 次试验的期望最大夏普 `E[max SR]` = **0.319**

## 2. 裁决

| | 检验 | 结果 | 预注册写好的结论 |
|---|---|---|---|
| **R1** | `mean(F) > 0.5` | ✘ **0.303** | 网格或样本不足以产生可观的挖掘溢价；须加大网格重跑，且在报告里如实写明设计被改过 |
| **R2** | `\|mean(P) − mean(N)\| < 0.30` | ✔ **0.143** | 预注册正确地拒绝了假发现：先封存参数后，样本外成绩与瞎选无异 |
| **R3** | `mean(F) − mean(P) > 0.5` | ✘ **0.494855** | 未观察到显著挖掘溢价 |
| **R4** | `mean(F) ≤ E[max SR]` | ✔ **0.303 ≤ 0.319** | Deflated Sharpe 当场判定 F 臂的成绩落在噪声预期之内 |

中止条件均未触发：`|mean(P)| = 0.192 < 0.5`（A1），仪器失效 0（A2）。

## 3. R3 差了 0.005145

阈值是 0.5，实测 0.494855。**差 0.005145，判不通过。**

这条单独拎出来说，因为它是本次运行里信息量最大的一条：

> 如果没有预注册，此刻这份报告会写「挖掘溢价约 0.5」，然后把 R3 记成成立。
> 阈值写在跑之前，所以它不成立。

**这就是预注册要防的那个动作本身**——不是防有人故意造假，是防写报告的人在一个临界值面前
不自觉地把话说圆。

## 4. 立得住的那一条

R4 成立，而它是四条里唯一直接回答原问题的：

> **F 臂 0.303 的夏普，落在「纯噪声下试 36 次预期能挖到的最大值」0.319 之内。**

真 alpha 已知为 0，所以这个 0.303 全部是挖出来的。Deflated Sharpe 不需要知道真 alpha
也判出了同一件事——**它只需要知道你试了多少次**。而试了多少次，只有预注册说得清。

R2 也成立：封存参数之后，样本外成绩（−0.192）与瞎选（−0.049）无异。

## 5. 未预注册的观察（不作结论）

P 臂的参数在样本内拿 **0.476**，同一组参数到样本外变成 **−0.192**，跌落 0.668。

这比 `F − P` 更直观地展示了过拟合，但**它没有被预注册为判据**，所以这里只记录，不当结论，
也不用它去补救 R1/R3 的不成立。若要用它，得写进下一份预注册再跑。

## 6. 对预注册的偏离（如实记录）

跑手改过一次，改的是**索引方式不是判据**：

- 首跑（[`RESULTS-run1.json`](RESULTS-run1.json)）按 `test` 字符串索引裁决，而 `prereg.json`
  的 R1 写的是 `"mean(F.sharpe) > 0.5 across seeds"`，多出的后缀匹配不上，R1 被报成
  **⚠ 规则未被跑手实现**（它没有默默跳过——那条警报正常工作了）
- 改为按规则 `id` 索引后重跑
- **两份结果除 `verdicts` 外全文逐位相同**，R1 由 `null` 变 `false`；两份都在仓库里，可 diff 自证
- 四条判据的阈值与方向一字未动
- 代价：`backtest.mjs` 的 sha256 因此变了，与锚点 `63d46b16fc99c421` 中记录的值不再一致。
  **这是系统该有的反应**——改了就露出来。原值 `7dc8ac54…` 仍写在被锚定的学历里

## 7. 边界（这份结果保证不了什么）

照抄 [`PREREGISTRATION.md` §7](PREREGISTRATION.md)，一条不减：

1. **合成随机游走 ≠ 真实市场。** 本实验证明审计工具能抓到一个我们自己种进去的假阳性，
   **不构成对任何真实策略的判决**，也不给出真实数据上的假阳性率
2. **数据是我们自己造的**——自己出的卷子考满分不算能力证据
3. 双均线是玩具策略，long-flat、单资产、5bp 固定成本；滑点、冲击、借券、涨跌停一概未建模
4. 20 个种子只够看数量级，不够支撑百分点级比较
5. 三条臂的实现出自同一作者

**另加一条本次暴露的**：R1 不成立说明这个网格（36 组）的挖掘空间偏小。预注册对此写了
处置方式（加大网格重跑并声明设计已改）——**那需要一份新的预注册和一次新的盖章，不能在
这份结果上续写。**

## 8. 陌生人怎么核验

```bash
node governance/anchor.mjs verify                      # 锚点是否与磁盘一致、有没有外部时间锚
ots verify governance/anchors/63d46b16fc99c421.json.ots  # 时间由比特币区块头给，不由我们给
git log --oneline                                      # a5e60eb 锚点在前，结果提交在后
node demo/quant-preregister/backtest.mjs               # 确定性重跑，数值应逐位相同
```

改 `prereg.json` 任何一个字节再跑，跑手会以退出码 3 拒绝——试一下就知道那不是自觉。
