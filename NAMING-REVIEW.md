# 命名评估 · naming/review-0.1

> 这份文件**只出方案，不改名**。写在分支上，主干不动。
> 结论先说：**「中文名 vs 英文名」不是这里的问题，它盖住了真问题。**

---

## 0. 先把三类东西分开

「命名不好」这句话在本仓库里其实混着三件性质完全不同的事：

| 类 | 是什么 | 判据 | 结论 |
|---|---|---|---|
| **A 概念名** | 本象 / 本境 / 影核 / 北桥 / 南桥 / 学堂 / 学历 | 它帮不帮你想清楚问题 | **不改** |
| **B 名实不符** | `southbridge/` 目录里装着三个协议的东西 | 声称的内容 ≠ 实际内容 | **是缺陷，该改，但不是现在** |
| **C 对外映射** | 英文读者遇到这些词该叫什么 | 有没有一份稳定的对照 | **已有 `TERMINOLOGY.md`，补齐即可** |

把 B 说成"中文名的问题"，会导致改了语言、缺陷还在。

---

## 1. A 类：概念名不改，三条理由

### ① 命名冻结在有效期内
`CLAUDE.md` 写着：**命名冻结：2Origin / 本象 / 本境 / 影核 / 北桥 / 南桥 / 学堂 —— 一个季度内不改。**
这条规则本身就是为了防止"每次觉得不顺眼就重命名"造成的搅动。现在推翻它，等于用一次口味判断
去否掉一条为防口味判断而立的规则。

### ② 这套隐喻在干活，不是装饰
最硬的一个证据：正因为把系统想成**主板**，才问得出「七个部件装了几个」，
才发现**唯一没装的是本象**——而那一问直接定位了"自证"病在本仓库复发五次的**结构性成因**
（RFC-0006 §0：每个部件自己当自己的观察者）。

换成 `StateStore` / `ActionKernel` / `ContextCompiler` / `Observer`，这是一组正确但**互不成套**的名字。
"七个部件里少了一个"这个问题问不出来，因为没有"一台主机"这个整体在那儿。

同理还有：`学历`（跨会话继承的是学籍，不是记忆）、`影核`（动作的成败由回头观察决定，"影"是自述、"核"是判决）、
`北桥/南桥`（知与行分离，且各有两个时刻/两条通道）。

### ③ 隐喻的失效点已经被记录过，而且是靠代码裁决的
架构文档自相矛盾过：第四章标题写「影核：南桥 + 驱动层」（影核 ⊃ 南桥），
正文却写「南桥找到影核：PowerPoint Driver」（影核 = 被调用的驱动）。
本仓库的处理不是改名，是**按代码定名**：`shadowcore-core.mjs` 导出
`assessRisk/checkApproval/doWrite/doVerify` → 影核 = action kernel（决定与执行），
南桥 = action channel（通道）。这条已写进 `TERMINOLOGY.md §1`。

**规矩因此是明确的：按代码里实际存在的东西定名，不按架构文档定名。** 这条规矩比名字本身值钱。

---

## 2. B 类：`southbridge/` 是一个抽屉，不是一个部件

### 实测

`southbridge/` 下 13 个 `.mjs`，分属**三个不同协议**：

| 归属 | 文件 |
|---|---|
| 本境 `2origin/0.2` | `benjing-core` `benjing-put` `benjing-todo` `schema-check` `verify-benjing` `verify-state` `verify-todo` |
| 影核 `shadowcore/0.2` | `shadowcore-core` `southbridge-cli` `southbridge-mcp` `verify-southbridge` `probe-append-idempotency` |
| 都不属于 | `uking-heartbeat` |

而 `TERMINOLOGY.md` 自己写着：**南桥 = action channel（通道），目前两条：CLI 与 MCP**。
也就是说——**这个目录里只有 2 个文件真的是南桥，另外 11 个不是。**

对照其他部件：`benxiang/` 3 个、`northbridge/` 2 个、`oob/` 3 个、`xuetang/` 3 个，
每个目录 = 一个协议。只有 `southbridge/` 是历史堆积形成的抽屉。

### 为什么这是缺陷而不是口味

目录名声称的内容与实际内容不符，就是**「声明冒充事实」在文件系统层的形态**——
和"没人加载的 schema"、"bundle 谎报装载量"、"幂等账本说 done 而文件已删"同型。
一个新来的人（或新来的模型）读 `southbridge/verify-benjing.mjs` 会先困惑三秒：
本境的验证器为什么在南桥目录里？那三秒就是这条缺陷的成本，每人每次。

### 迁移方案（**不执行**，只标价）

```
southbridge/  →  benjing/     本境：benjing-core / benjing-put / benjing-todo /
                              schema-check / verify-benjing / verify-state / verify-todo
              →  shadowcore/  影核：shadowcore-core / southbridge-cli / southbridge-mcp /
                              verify-southbridge / probe-append-idempotency / audit.log / idempotency.*
              →  tools/       uking-heartbeat（本来就不属于任何协议）
```

**爆炸半径（实测计数，不是估计）：**

| 位置 | 出现次数 |
|---|---|
| 代码 import / 路径 | 42 |
| 文档与 RFC 里的命令行 | 74 |
| **学历 `facts[].source` 里** | **108** |
| 两个公开仓库 | 34 |
| 合计 | **258** |

第三行是真正的拦路虎：`source` 里的路径是**证据指针**。批量替换会让
`dereferenceSource` 的悬空探针大面积告警，而且**改学历里的 source 等于改证据**——
本仓库有明确结论：`facts` 的 `source` 必须引用可复核物；重写 258 处路径而不逐条复核，
就是把"可复核"降级成"看起来像可复核"。

### 建议的时机与做法

`demo/task8` 已经记着：**「southbridge/ 目录名实不符：等有一次非做不可的改动时顺手做，
改的时候用 B12 悬空报告逐条修 source」**——这个判断到今天仍然成立，本评估只是把它量化了。

真要做，顺序必须是：

1. 先给 `dereferenceSource` 加一条**路径别名表**（`southbridge/x.mjs` → `benjing/x.mjs`），
   让旧 source 在迁移期仍能解引用 —— **先保证证据不断链，再动文件**；
2. `git mv` 分三批（本境 / 影核 / tools），每批单独提交，每批后跑全量 210 条判据；
3. 用 B12 悬空报告**逐条**确认 108 处 source，不做全局 sed；
4. 最后才更新两个公开仓库与 4 份 RFC。

**在这轮之前不要动。** 现在动，等于在没有别名表的情况下让 108 条已验证事实的证据指针同时失效。

---

## 3. C 类：对外映射已经有了，补齐即可

`TERMINOLOGY.md`（83 行）已经是那份"中英提案"，而且规矩定得对：

> 内部中文名**冻结不动**。这份表只解决一件事：英文读者、论文审稿人和公开仓库的读者，
> 遇到这些部件时该叫它什么。**命名冻结管的是内部一致性，对外映射是另一件事，两者不冲突。**

缺口只有两个，本分支已补：

1. **学堂 / 经验 / 考试** 是今天新增的，主表里没有（原文只在别处提过一次）；
2. 没有一节写明"哪些名字现在是**债**"——即 B 类。读者看不到 `southbridge/` 名实不符这件事。

---

## 4. 一句话结论

- **不要为了"中文名不好"去改名**：概念名在冻结期内，而且它们在帮你想问题。
- **要改的是 `southbridge/` 这个抽屉**：它是缺陷不是口味，但爆炸半径 258 处，其中 108 处是证据指针。
- **对外用 `TERMINOLOGY.md`**：中文名做内部规范，英文名做对外接口，两套并行不冲突。
