补学堂最大的洞（改成可测量），并修一个我自己造成的 provenance 事故

一、RFC-0008 §6「挂恒绿命令就能骗过考试」——不假装解决，改成可测量
  discriminating(l)：一条考题只有被观察到红过至少一次（exam.fails>0）才算证明
  了判别力，判据取自考试账本，不取任何自称字段。考试每轮单独报「未证明」条数
  并逐条列出——不报这个数，「6 条经验通过考试」会被读成「6 条被证明为真」，
  与判分器不报 finish_reason=length 是同一条规矩。
  首轮实测 2/6：只有两条考题曾真的红过（正是 schema 改动打破 verify-benjing
  夹具那次）。这个 2/6 就是「学堂已验证 6 条经验」的真实成色。
  刻意不做：不因未证明拒绝升 verified（第一天就卡死 4/6，且"没红过"≠"假考题"）；
  不做变异测试（代价与收益不成比例）。选择写进 RFC 而不是藏起来。

二、堵 git 化引入的写路径：hooks/branch-safety.mjs
  git checkout/stash/reset 会覆盖工作区的学历，绕过本境乐观锁（只管 putState）
  也绕过 PreToolUse 守卫（只管 Write/Edit）。precheck 切前拦、post-checkout 切后
  如实报告。真正的脚本在版本控制里，.git/hooks 只放转发器——否则「钩子装没装」
  又会变成只有本机知道的事实。它是约定不是强制点，已写进 RFC §6。

三、我自己造成的 provenance 事故
  putState 写的是 `actor || detectActor()`，学堂考试传了字符串标签
  'xuetang/exam'，一轮把 8 份学历观测到的 harness/model/model_source 全盖掉。
  actor 存在的理由就写在 schema 里：跨模型/跨 harness 继承是核心主张，没有
  provenance 就无从举证。
  更该记的是：我先前把它说成「先于本轮存在的 schema 漂移」并据此推迟——
  核对 demo/.benjing-backups/ 每份最早备份，actor 全是 object(harness=claude-code)，
  证明是今天写坏的。把自己的破坏归给历史，正是这个仓库要修的那个病。
  修法改数据也改写入方：调用方给的只能是标签，观测值永远保留；B13.1/B13.2
  两条判据锁死（本境 65 → 67）。

回归：七协议全绿（本象14/北桥19/本境67/影核43/带外29/待办14/学堂29，共 215 条，
其中学堂 19 条反向），11/11 学历 VERIFIED，schema 11/11 合规。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
