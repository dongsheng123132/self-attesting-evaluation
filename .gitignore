# 聊天记录是「影」，不是状态 —— 按本仓库铁律，trace 是学堂原料，不进版本控制。
# 它同时也是私人工作内容：整份会话逐轮文本，公开出去与 corpus.json 的取舍自相矛盾。
.claude/trace.jsonl

# 每会话的装载账本 / 缓存体检 / 锁文件：派生物与临时态，可由代码重算
.claude/nb-injected-*.json
.claude/benjing-health.json
.claude/scheduled_tasks.lock

# 临时中转文件（本仓库多次用 *.tmp.json / *.tmp.mjs 做 benjing-put 的入参）
*.tmp.json
*.tmp.mjs
node_modules/

# 真实客户工作区：活状态可以继续由本境管理，但原稿、姓名、绝对路径和生成物不进入公开代码历史。
# 可复用规则应从这里提炼到协议/工具目录；对外证据必须走 governance/export-state.mjs。
demo/book-project/
demo/.benjing-backups/*-book-project-v*.json
private/

# 南桥写入探针/覆盖备份是本机运行态；跨模型论文证据使用下面明确保留的账本与本境备份。
southbridge/.backups/

# 证据锚定的工作副本：每次 build 覆盖，且账本天天在长所以它天天变。
# 它可由 `node governance/anchor.mjs build` 从磁盘确定性重算，进 git 只会制造噪音。
# 有价值的是 governance/anchors/ 里那些冻结快照 + .ots（见下方"刻意不忽略"）。
governance/anchor-manifest.json

# Office/CAD/Python 临时物
~$*
__pycache__/
*.pyc

# ── 刻意**不**忽略的（写下来，免得以后有人"顺手"加进去）──
# southbridge/audit.log        只追加审计日志。ACCEPTANCE.md §2 的跨模型复算全靠它，
#                              第三方要能按行切片复原当时口径，删了这条主张就没了凭证。
# demo/.benjing-backups/       论文 self-attesting-evaluation.md 案例 8~13 的唯一凭证。
#                              论文 §7 已如实写明「这些凭证由被记录者自己产生」——
#                              进了 git 之后，那条弱点才算真的被修掉。
# benxiang/observations.jsonl  「上次被独立观察是什么时候」的唯一答案来源。
# governance/anchors/          冻结的证据快照 + OpenTimestamps 证明。这是本仓库唯一
#                              **不由我们自己签发**的时间凭证：本机 commit date 可以任意伪造，
#                              比特币区块头不行。删掉它们等于把优先权主张退回自证状态。
# demo/hongloumeng-c/corpus/wikisource-honglou-120.txt
# demo/hongloumeng-c/corpus/zhipi-raw.json
#                              C 轨判分器**运行时读的就是这两份**（judge.mjs / verify-zhipi-spec.mjs），
#                              而它们是本机加工出来的派生物，没有「一个 URL 取回原样」的重建路径
#                              （第一版建表时把 120 条回目全删了，现在这份是修过的第二版）。
#                              其余三轨的语料能忽略，是因为 fetch-corpus.mjs 真能把它们取回来；
#                              这两份不能，忽略它们等于把 C 轨全部结论变成不可复跑。5.3M，认了。
*.tmp

# 文学基准三轨的语料：公有领域原文，体积大且可由 demo/fetch-corpus.mjs 按指纹重建
demo/ulysses-19/corpus/
demo/spenser-7-1/corpus/
demo/holmes-untold/corpus/

# C 轨语料按「能不能重建」分开处置，不按目录一刀切 —— 详见 demo/hongloumeng-c/CORPUS-SOURCES.md。
# 这四份是 Gutenberg 原样文件，已进 fetch-corpus.mjs 的清单，按指纹可重建：
demo/hongloumeng-c/corpus/pg23863.txt
demo/hongloumeng-c/corpus/pg23962.txt
demo/hongloumeng-c/corpus/pg24032.txt
demo/hongloumeng-c/corpus/pg24264-honglou.txt
# 盲测副本是 wikisource-honglou-120.txt 的**逐字节前缀**（前 1,776,208 字节 = 前八十回），
# 由 `head -c 1776208` 可确定性重建，不必存两份：
demo/hongloumeng-c/handoff/blind/corpus/
# 同事的续编正文：是别人的作品，不是我们的。判决、指纹与统计留在仓库里，正文不进。
# 这与 PUBLICATION-POLICY.md §2「客户真实工作区」同一条理由。
demo/hongloumeng-c/external/

# 隐藏判据集 H：只提交 SEAL.json（哈希）与构造脚本，H 本身不进 git
demo/holmes-untold/hidden/

# 会话导出属私人工作内容，按本文件第一条铁律不进版本控制（历史剥离见 governance/publish.mjs）
ChatGPT-*.md

# M0 对照的真值（校验串/窗口/正确答案）：跑之前泄露即实验作废
demo/control-m0/.truth.json
